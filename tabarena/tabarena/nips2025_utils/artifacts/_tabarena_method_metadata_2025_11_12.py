# This dated file is intentionally empty: the original `contexttab_metadata`
# (now `sap_rpt_oss_method_metadata`) and `realtabpfn25_metadata`
# (now `realtabpfnv25_method_metadata`) live in their per-model `info.py`
# modules. Kept as a placeholder so import paths referencing this file's
# historical name don't break unexpectedly.
