# This dated file is intentionally empty: the original `perpetualbooster_metadata`,
# `tabpfn26_metadata`, and `tabstar_metadata` (now `perpetual_booster_method_metadata`,
# `tabpfnv26_method_metadata`, and `tabstar_method_metadata`) live in their per-model
# `info.py` modules.
