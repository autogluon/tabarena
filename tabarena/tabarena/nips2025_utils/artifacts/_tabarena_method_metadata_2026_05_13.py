# This dated file is intentionally empty: the original `limix_metadata`,
# `orionmsp_metadata`, and `tabpfnv3_method_metadata` (now `limix_method_metadata`,
# `orionmsp_method_metadata`, and `tabpfn_3_method_metadata`) live in their
# per-model `info.py` modules.
